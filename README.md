# Quantum autoencoder for anomaly detection on LHC Olympics 2020

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python](https://img.shields.io/badge/Python-3.10%2B-blue)](https://www.python.org/)
[![Qiskit](https://img.shields.io/badge/Qiskit-2.x-purple)](https://qiskit.org/)
[![PennyLane](https://img.shields.io/badge/PennyLane-0.4x-teal)](https://pennylane.ai/)

An unsupervised trash-qubit quantum autoencoder for dijet anomaly detection,
measured against classical autoencoders, PCA, and a single-variable cut, on the
standard **LHC Olympics 2020 R&D** benchmark.

The point of the project is the comparison, not the quantum model: the question
asked is whether a few-dozen-parameter variational circuit buys anything over
classical baselines given the same inputs, the same training loop, and the same
evaluation. The answer is reported whichever way it comes out.

---

## Results

**The headline is a null result: this quantum autoencoder shows no measurable
advantage over classical baselines, and no model in the study is statistically
distinguishable from any other.**

<!-- BEGIN GENERATED RESULTS -->
<!-- generated by `python -m src.report --write-readme`; do not edit by hand -->

| model | trainable par. | ROC-AUC | 1/ε_B at ε_S=0.3 | ρ(score, m_JJ) |
|---|---:|---|---:|---:|
| **Quantum AE** (RY encoding) | 36 | 0.6120 ± 0.0632 | 5.4 ± 1.5 | +0.025 |
| Quantum AE (ZZFeatureMap) | 36 | 0.5976 ± 0.0316 | 5.0 ± 0.8 | +0.019 |
| Classical AE, parameter-matched | 36 | 0.5803 ± 0.0074 | 4.2 ± 0.2 | +0.023 |
| Classical AE, 6-16-4-16-6 | 362 | 0.6286 ± 0.0913 | 5.5 ± 3.3 | +0.035 |
| PCA, 4 components | 24 | 0.5678 ± 0.0041 | 4.0 ± 0.1 | +0.028 |
| Cut on $m_{J_1}$ alone (no training) | 0 | 0.5812 ± 0.0002 | 2.6 ± 0.0 | +0.084 |

Mean ± sample s.d. over 5 seeds (0, 1, 2, 3, 4); each seed re-draws the data split and re-initialises every model.

**Are any of these differences real?**

| comparison (ROC-AUC) | mean difference | t | p | significant? |
|---|---:|---:|---:|---|
| QAE vs. a plain cut on $m_{J_1}$ | +0.0308 ± 0.0283 | 1.09 | 0.337 | no |
| QAE vs. the provably-incompressible encoding | +0.0144 ± 0.0379 | 0.38 | 0.724 | no |
| QAE vs. classical AE at equal parameters | +0.0317 ± 0.0268 | 1.18 | 0.302 | no |
| QAE vs. the larger classical AE | -0.0166 ± 0.0477 | -0.35 | 0.745 | no |
| larger classical AE vs. $m_{J_1}$ cut | +0.0474 ± 0.0408 | 1.16 | 0.309 | no |

| comparison (1/ε_B at ε_S=0.3) | mean difference | t | p | significant? |
|---|---:|---:|---:|---|
| QAE vs. a plain cut on $m_{J_1}$ | +2.80 ± 0.67 | 4.15 | 0.014 | **yes** |
| **incompressible** encoding vs. $m_{J_1}$ cut | +2.38 ± 0.36 | 6.60 | 0.003 | **yes** |
| classical AE (matched) vs. $m_{J_1}$ cut | +1.61 ± 0.08 | 20.66 | 0.000 | **yes** |
| PCA vs. $m_{J_1}$ cut | +1.45 ± 0.03 | 47.75 | 0.000 | **yes** |
| QAE vs. classical AE at equal parameters | +1.19 ± 0.64 | 1.84 | 0.139 | no |
| QAE vs. the incompressible encoding | +0.42 ± 0.88 | 0.47 | 0.660 | no |

Two-sided paired t-tests across seeds. Within a seed every model sees exactly the same events, so pairing removes split-to-split variation and is more sensitive than comparing independent mean ± s.d. summaries. With 5 seeds the power is low: *not significant* means **not resolved by this study**, not *identical*.

Note the two metrics disagree. No model separates from any other on AUC, but every multivariate score beats the single-variable $m_{J_1}$ cut on rejection at a fixed working point — **including the ZZFeatureMap model that provably cannot compress**. Beating the mass cut therefore demonstrates only that more than one feature is being used; it is not evidence that the autoencoder, quantum or classical, is doing anything further.

| encoding | max P(trash=\|00⟩) over **all** ansätze | effective dim of ρ | verdict |
|---|---:|---:|---|
| ZZFeatureMap | 0.342 (12% of available headroom) | 59.4 of 64 | **not usefully compressible** |
| RY angle encoding | 0.872 (83% of available headroom) | 7.8 of 64 | compressible |

Random-guess reference: 0.250.

Configuration: 6 qubits (4 latent + 2 trash), ansatz reps {'zz': 5, 'ry': 5} (selected on background validation loss from [1, 3, 5]), 100,000 background training events, 60 max epochs. noiseless statevector, infinite shots. Wall time 2981 s.
<!-- END GENERATED RESULTS -->

### What the numbers say

1. **Nothing separates on AUC.** Every paired comparison returns p > 0.19. The
   QAE is not distinguishable from a single-variable cut on m_j1 (p = 0.34),
   from a classical autoencoder at identical parameter count (p = 0.30), or
   from the larger dense AE (p = 0.74).

2. **The decisive control: the QAE is indistinguishable from an encoding that
   provably cannot compress.** `qae_zz` is bounded by
   [Ky Fan](#finding-zzfeaturemap-cannot-compress-this-data-for-any-ansatz) at
   0.342, barely above the 0.25 random-guess value, and reaches only 12% of the
   usable headroom. It nonetheless scores AUC 0.598 ± 0.032 against the working
   model's 0.612 ± 0.063 (p = 0.72). If a model that cannot compress performs
   the same as one that can, the anomaly-detection performance is not coming
   from the compression.

3. **Seed variance dominates the model differences.** The dense AE spans
   0.536 → 0.765 across five seeds on identical data and hyperparameters —
   a 0.23 swing from initialisation alone, far larger than any gap between
   models. The untrained baselines are stable to ±0.004 (PCA) and ±0.0002
   (m_j1 cut), so the instability is a property of the training, not the data.

4. **AUC and rejection disagree, and the disagreement is informative.** Every
   multivariate score *does* significantly beat the m_j1 cut on background
   rejection at ε_S = 0.3 — but so does the incompressible `qae_zz`
   (p = 0.003). That gap therefore measures "uses more than one feature",
   not "the autoencoder learned something".

5. **The one clear win: the learned scores are mass-decorrelated, the mass cut
   is not.** All learned scores give |ρ(score, m_JJ)| ≲ 0.035 on background and
   their top-1% selections track the inclusive m_JJ spectrum closely. The m_j1
   cut does not — it reaches ρ = 0.084 and visibly distorts the spectrum
   (`results/figures/sculpting.png`: suppressed below ~2500 GeV, shifted
   right). For a bump hunt that difference matters more than a few points of
   AUC, because a sculpted background can manufacture a fake excess. Holding
   m_JJ out of the inputs worked as intended.

   This is a property of using decorrelated substructure inputs, though, not of
   the quantum model: the classical AE and PCA achieve it equally.

**What would change this conclusion:** more seeds (5 gives very low power — a
real effect of this size would need ~30+), more qubits and features, or a
signal-model-independent evaluation. The result here is "no effect resolved",
not "no effect exists".

Headline metrics are ROC-AUC and background rejection 1/ε_B at fixed signal
efficiency ε_S. Both are independent of the signal fraction mixed into the test
set. Training convergence and wall-clock time are diagnostics, not results.

### Figures

| file | what it shows |
|---|---|
| `results/figures/auc.png` | ROC-AUC with across-seed error bars — the honest headline: every bar overlaps |
| `results/figures/roc.png` | background rejection vs signal efficiency (one seed; orderings are not stable) |
| `results/figures/sculpting.png` | background m_JJ spectrum, inclusive vs the most anomalous 1% |
| `results/figures/scores.png` | anomaly-score distributions, background vs signal |
| `results/figures/training_curves.png` | train/validation loss per model |
| `results/figures/circuit.png` | the Qiskit circuit actually used |

---

## Finding: ZZFeatureMap cannot compress this data, for any ansatz

The obvious QML choice for encoding — Qiskit's `ZZFeatureMap` — provably cannot
work in a trash-qubit autoencoder here, and the reason is structural rather than
a matter of tuning.

`ZZFeatureMap` applies a Hadamard to every qubit and then **only phase gates**.
Every amplitude therefore has modulus 2^(−n/2) for *every* input; distinct
events differ only in phase, are close to mutually orthogonal, and span the full
Hilbert space.

This admits an exact, ansatz-independent bound. A trash-qubit autoencoder
applies one fixed unitary *U* and asks for the trash register to read |0…0⟩,
which means mapping states into a 2^n_latent-dimensional subspace. Writing
ρ = 𝔼ₓ|ψ(x)⟩⟨ψ(x)| for the average encoded state,

```
E_x[ P(trash = |0..0>) ]  =  tr( rho · U† Π U )
max_U E_x[ P(trash = |0..0>) ]  =  sum of the k largest eigenvalues of rho
```

with k = 2^n_latent, by [Ky Fan's theorem](https://en.wikipedia.org/wiki/Ky_Fan#Ky_Fan.27s_theorem).
This is a ceiling no ansatz, depth, or training budget can exceed. For a
maximally mixed ρ it equals k/2ⁿ — exactly the random-guess probability.

`src/encoding_analysis.py` computes it. Both encodings are trained and reported
side by side so the failure is visible rather than quietly omitted.

This is why the model uses **RY angle encoding** (data in amplitudes) rather
than `ZZFeatureMap` (data in phases).

---

## Data

[LHC Olympics 2020 R&D dataset](https://doi.org/10.5281/zenodo.6466204)
(Kasieczka, Nachman & Shih), Zenodo record 6466204 v5, CC-BY 4.0.

* 1 M QCD dijet background + 100 k signal events
* Signal: W′ → XY, X→qq, Y→qq, with m_W′ = 3.5 TeV, m_X = 500 GeV, m_Y = 100 GeV
* Pythia 8 + Delphes 3.4.1, no pileup or MPI, single fat-jet trigger p_T > 1.2 TeV

Only the **74 MB high-level feature file** (`events_anomalydetection_v2.features.h5`)
is used — jets already clustered anti-k_T R=1 by the dataset authors. The 2.9 GB
raw hadron-level file is **not** needed, which is what makes this run on a free
Colab tier. `src/data.py` downloads it and verifies the md5.

### Inputs

Six features: `mj1`, `Δmj`, `τ21^j1`, `τ21^j2`, `τ32^j1`, `τ32^j2`,
with jets ordered by **mass** (j1 = lighter), so Δmj ≥ 0 by construction.

**m_JJ is deliberately excluded from the inputs.** It is the resonance variable
a real search bump-hunts in, so an anomaly score that depends on it distorts the
spectrum the search relies on. It is retained solely as a sculpting diagnostic —
the reported ρ(score, m_JJ) is the Spearman correlation on background events.
This follows the convention of the ANODE / CATHODE / CWoLa-hunting work on this
dataset.

Features are mapped to [0, π] by a `QuantileTransformer` fit on the **background
training split only**. Quantile rather than min-max because m_j1 has a long tail
that min-max would crush into a narrow angular range. The transform is monotone
per feature, so it cannot change the AUC of any single-feature baseline.

---

## Model

Trash-qubit quantum autoencoder ([Romero, Olson & Aspuru-Guzik,
arXiv:1612.02806](https://arxiv.org/abs/1612.02806); applied to HEP by
[Ngairangbam, Spannowsky & Takeuchi, PRD 105, 095004](https://arxiv.org/abs/2112.04958)):

```
|x> --[ RY angle encoding ]--[ RealAmplitudes(theta), reps ]--  4 latent qubits (kept)
                                                                2 trash qubits (measured)
```

* **6 qubits**, 4 latent + 2 trash; `n_qubits × (reps + 1)` trainable parameters
* Loss `L = 1 − P(trash = |00⟩)`, averaged over **background only**
* The same quantity per event is the anomaly score at test time
* No SWAP test: measuring the trash register directly is equivalent to the
  SWAP-test fidelity against a fresh |00⟩ reference and costs 3 fewer qubits.
  This is the standard simplification and is what Ngairangbam et al. use.

**Ansatz depth is not fixed a priori.** It is selected from `{1, 3, 5}` by
**background validation loss** — no labels, no signal events, no test data.
That is model selection an unsupervised practitioner can legitimately do;
selecting depth by test AUC would be tuning on the evaluation set. The selected
depth and the full selection scan are recorded in `results/metrics.json`.

One caveat worth stating: validation compression loss is a *proxy* for anomaly
detection, not the same thing. A model that compresses background better is not
guaranteed to separate signal better — it could in principle compress signal
well too. The criterion is defensible because it is label-free, not because it
is optimal.

### Why PennyLane trains it and Qiskit specifies it

Qiskit's gradients use the parameter-shift rule: 2 circuit evaluations per
parameter per step. At a few dozen parameters and 10⁵ training events that is
~10⁶ circuit executions per epoch, which is not viable on a free Colab CPU.

PennyLane's `default.qubit` with `diff_method="backprop"` differentiates through
the statevector simulation directly, giving the *same* exact noiseless gradients
in seconds per epoch.

The Qiskit circuit remains the specification, and `verify_against_qiskit()`
asserts that the two implementations produce the **same statevector to ~10⁻¹⁶**
(accounting for Qiskit's little-endian vs PennyLane's big-endian convention).
That check runs at the start of every study, for both encodings.

---

## Baselines

Four, deliberately, all on identical inputs and the same training loop
(`src/train.py`) with the same optimiser, schedule, batching and early stopping:

| baseline | why it is here |
|---|---|
| Tied-weight AE, **parameter-matched** | exact match to the QAE's parameter count |
| Dense AE, 6-16-4-16-6 (362 par.) | what a practitioner would actually build |
| PCA, 4 components | linear reconstruction error, hard to argue with |
| **Cut on m_j1 alone** | the control that matters most |

The matched AE is **built to order at run time**, because the QAE's parameter
count is `n_qubits × (reps + 1)` for whatever depth gets selected, not a fixed
number. `matched_ae()` searches tied-AE configurations (latent size, optional
output affine, optional latent bias) for one with *exactly* the target count,
preferring the latent dimension closest to the quantum model's, and raises
rather than silently reporting an unmatched "matched" baseline.

Reporting only the matched AE would look like handicapping the classical side;
reporting only the dense AE would be unfair to the QAE. Both are given.

The m_j1 control is the one to look at first. Autoencoders on jet features are
known to score largely as a proxy for jet mass, so **if the QAE does not beat a
plain threshold on m_j1, there is no result here** — and the README says so.

**On "matched parameter count":** the QAE keeps 4 of 6 qubits, so the classical
models keep 4 of 6 dimensions. That matches retained degrees of freedom. It does
*not* match Hilbert-space dimension (4 qubits span 16 complex dimensions). No
single notion of "same size" applies across the two model families, which is
precisely why several baselines are reported instead of one.

---

## Limitations

Stated plainly, because they bound what the numbers mean:

1. **Noiseless statevector simulation, infinite shots.** No hardware noise, no
   shot noise, no decoherence, no connectivity constraints. These results say
   nothing about performance on real quantum hardware.
2. **6 qubits, 6 features.** A small slice of the available information.
   Published LHCO analyses use far more, including full jet constituents.
3. **Subsampled training set** — 100 k of the 1 M background events by default.
   All remaining background goes to the test set to maximise the statistics
   available for rejection at fixed efficiency.
4. **Barely tuned.** Only ansatz depth is selected (on background validation
   loss, from three candidates). Latent size, learning rate, batch size and the
   classical architectures were fixed a priori. A properly tuned QAE might do
   better — so might a properly tuned classical AE, and the classical side was
   given no comparable search. Read the comparison as "two lightly-tuned models
   under matched conditions", not as best-versus-best.
5. **The input scaling is a real choice, not a neutral one.** A
   `QuantileTransformer` fit on background makes the background marginals
   uniform, which suits angle encoding. It is applied identically to every
   model and fit only on training background, and being monotone per feature it
   cannot change any single-feature baseline's AUC — but a different scaler
   would give different absolute numbers.
6. **Not comparable to the official LHCO leaderboard.** That challenge scored
   *black-box* bump hunts by discovery significance, mostly with weakly
   supervised methods. This is unsupervised anomaly detection on the R&D
   dataset — the same data, a different task. Comparisons should be made against
   published unsupervised autoencoder numbers, not the challenge results.
7. **Unsupervised autoencoders are not state of the art on LHCO.** Weakly
   supervised approaches substantially outperform them. No SOTA claim is made.
8. **Detector realism is whatever Delphes provides.** No pileup, no MPI, no
   systematic uncertainties.
9. Ngairangbam et al. report a QAE outperforming a classical AE, but on a
   *different* dataset (t̄t background, heavy Higgs signal) with different
   inputs. Their result does not transfer to this benchmark and is not evidence
   for the numbers here.

---

## Reproducing

### Colab

Open `notebooks/QAE_LHCO.ipynb`. It clones the repo, installs dependencies,
downloads the data, and runs the study. Roughly 30–45 min on a free CPU
instance; no GPU is required (6 qubits = 64 amplitudes, and the bottleneck is
Python overhead rather than arithmetic).

### Locally

```bash
pip install -r requirements.txt
python -m src.run_study --seeds 0 1 2 3 4
python -m src.report
```

### Docker

```bash
docker build -t qae-lhco .
docker run --rm -v "$(pwd)/results:/app/results" -v "$(pwd)/data:/app/data" qae-lhco
```

Mounting `data/` caches the 74 MB feature file so Zenodo is only hit once.

*Note: Zenodo is intermittently unavailable. `src/data.py` retries with
exponential backoff and fails with an explicit message rather than silently.*

---

## Layout

```
src/data.py                 download + md5 verify, feature construction, splits
src/qae.py                  circuits, torch module, Qiskit equivalence check
src/encoding_analysis.py    ansatz-independent compressibility bound
src/train.py                one training loop, shared by every model
src/baselines.py            classical AEs, PCA, single-feature control
src/evaluate.py             AUC, rejection at fixed efficiency, sculpting
src/figures.py              figure generation
src/report.py               generates the README results table
src/run_study.py            orchestration, multi-seed
notebooks/QAE_LHCO.ipynb    Colab entry point
```

## References

* Kasieczka, Nachman & Shih. *R&D Dataset for LHC Olympics 2020.* [doi:10.5281/zenodo.6466204](https://doi.org/10.5281/zenodo.6466204)
* Kasieczka et al. *The LHC Olympics 2020: A Community Challenge for Anomaly Detection in High Energy Physics.* [arXiv:2101.08320](https://arxiv.org/abs/2101.08320)
* Romero, Olson & Aspuru-Guzik. *Quantum autoencoders for efficient compression of quantum data.* [arXiv:1612.02806](https://arxiv.org/abs/1612.02806)
* Ngairangbam, Spannowsky & Takeuchi. *Anomaly detection in high-energy physics using a quantum autoencoder.* [PRD 105, 095004](https://arxiv.org/abs/2112.04958)

## License

MIT.
