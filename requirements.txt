# Core. Version floors, not hard pins, so this installs cleanly on Colab.
# Tested with the versions given in the trailing comments.
pennylane>=0.40          # 0.45.1   training backend (backprop through statevector)
qiskit>=1.2              # 2.5.2    circuit specification + cross-check
torch>=2.2               # 2.14.0   autograd and optimiser
numpy>=1.26              # 2.5.3
pandas>=2.0              # 3.0.5    reads the LHCO HDF5 feature file
tables>=3.9              # 3.11.1   pandas HDF5 backend (pd.read_hdf)
scikit-learn>=1.3        # 1.9.0    QuantileTransformer, PCA, ROC metrics
scipy>=1.11              # 1.18.1   Spearman correlation
matplotlib>=3.7          # 3.11.1   figures

# Optional: only needed to render the circuit diagram with Qiskit's mpl drawer.
pylatexenc>=2.10

# Note on Qiskit 2.x: the class-based ZZFeatureMap / RealAmplitudes were
# deprecated in 1.3 and the function forms (zz_feature_map, real_amplitudes)
# are used here. real_amplitudes defaults to entanglement="reverse_linear";
# this project passes entanglement="linear" explicitly.
